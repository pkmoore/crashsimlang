tmp <- 4;
ftmp <- 3.5;
ftmp2 <- 3.2;
ntmp <- -1;
ntmp2 <- -0.2;

numnum <- 6 / 2;
regnum <- tmp / 4;
numreg <- 4 / tmp;
regreg <- tmp / tmp;

fnumnum <- 7 / 2;
fregnum <- ftmp / 2;
fnumreg <- 10 / ftmp2;
fregreg <- tmp / tmp;

nnumnum <- 5 / -1;
nregnum <- ntmp / 5;
nnumreg <- 5 / ntmp;
nregreg <- ntmp / ntmp2;
