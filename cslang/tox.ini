[tox]
envlist = py2.7

[testenv]
usedevelop = true
deps =
  pytest
  coverage
  dill
  ply
  git+https://github.com/pkmoore/posix-omni-parser
commands =
   coverage run --source=cslang -m pytest
   coverage report -m
